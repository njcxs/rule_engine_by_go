state: enable
rule_id : sqli_get_01
rule_tag: sqli
rule_name: sqli_get_select
rule_type: and

detect_list:
  - field : network.ip
    type: re
    rule: xxx
    ignorecase: False
  - field : network.ip
    type: equal
    rule : xxx

  - field : network.ip,
    type: in
    rule : 100


threat_level : high
auth : njcx
info": "about sql injection attack"

e-mail:
    - xxx
    - xxx

